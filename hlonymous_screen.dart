import 'package:flutter/material.dart';

class Hlonymous extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return Center(
      child: Text('Hlonymous'),
    );
  }
}